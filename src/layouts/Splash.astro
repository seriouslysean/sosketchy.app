---
import Base from './Base.astro';

interface Props {
  title?: string;
  description?: string;
  noindex?: boolean;
  referrer?: string;
}

const { title, description, noindex, referrer } = Astro.props;
---

<Base title={title} description={description} noindex={noindex} referrer={referrer}>
  <main id="main">
    <slot />
  </main>
  <slot name="footer" />
</Base>
