---
import { APP_NAME, BUSINESS_NAME } from 'astro:env/client';
import { getFullUrl } from '../utils/url-utils';

const siteRoot = getFullUrl('/').replace(/\/$/, '');

const graph = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': `${siteRoot}/#org`,
      name: BUSINESS_NAME,
      url: siteRoot,
    },
    {
      '@type': 'WebSite',
      '@id': `${siteRoot}/#site`,
      url: siteRoot,
      name: APP_NAME,
      inLanguage: 'en',
      publisher: { '@id': `${siteRoot}/#org` },
    },
    {
      '@type': 'SoftwareApplication',
      '@id': `${siteRoot}/#app`,
      name: APP_NAME,
      applicationCategory: 'GameApplication',
      operatingSystem: 'iOS 17',
      url: siteRoot,
      image: getFullUrl('/og-image.png'),
      creator: { '@id': `${siteRoot}/#org` },
    },
  ],
};
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(graph)}></script>
