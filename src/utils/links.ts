// The App Store product page. Apple issues the numeric ID when the listing is
// created and it never changes, so it is checked in rather than injected: an
// unset CI variable must not be able to unpublish the install link.
export const APP_STORE_URL = 'https://apps.apple.com/app/id6770168860';
