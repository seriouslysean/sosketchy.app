SITE_URL=https://example.com
# Subdirectory the site serves from. "/" for root (custom domain),
# or "/repo-name" for default GitHub Pages project sites.
# BASE_PATH=/

# Brand values are optional. Set locally in .env; set in production via
# GitHub Actions repository variables (Settings → Secrets and variables →
# Actions → Variables).
APP_NAME=
BUSINESS_NAME=
BUSINESS_URL=
SUPPORT_EMAIL=
APP_STORE_URL=
